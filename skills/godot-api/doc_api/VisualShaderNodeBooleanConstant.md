## VisualShaderNodeBooleanConstant <- VisualShaderNodeConstant

Has only one output port and no inputs. Translated to [code skip-lint]bool[/code] in the shader language.

**Props:**
- constant: bool = false

- **constant**: A boolean constant which represents a state of this node.

