## Compositor <- Resource

The compositor resource stores attributes used to customize how a Viewport is rendered.

**Props:**
- compositor_effects: CompositorEffect[] = []

- **compositor_effects**: The custom CompositorEffects that are applied during rendering of viewports using this compositor.

