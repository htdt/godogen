## JointLimitationCone3D <- JointLimitation3D

A cone shape limitation that interacts with ChainIK3D.

**Props:**
- angle: float = 1.5707964

- **angle**: The radius range of the hole made by the cone. `0` degrees makes a sphere without hole, `180` degrees makes a hemisphere, and `360` degrees become empty (no limitation).

