## One-shot delivery

This is an unattended build — the user is not watching. Bias toward finishing over asking, and make reasonable calls on anything ambiguous. The deliverable is the finished project plus a **15–20s screen video recording of the running game** as proof it works. 
